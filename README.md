input text
input key length (how many digits of PI)
turns text into ascii and encrypts with ceaser shift
reverts to text and prints encrypted text
decrypts ascii
revert to text and print decrypted text
